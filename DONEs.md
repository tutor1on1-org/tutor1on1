# DONES
Last updated: 2026-04-30

Recent completions only. Detailed historical delivery records live in `LOGBOOK.md`.

- Fixed OpenAI Codex/ChatGPT OAuth tutor calls failing with HTTP 400 `Unsupported parameter: max_output_tokens` by omitting that field on the OAuth provider request path and adding request-shape regression coverage.
- Fixed OpenAI Codex/ChatGPT OAuth tutor calls failing with HTTP 400 `Instructions are required` by sending a concise top-level Codex `instructions` field and adding request-shape regression coverage.
- Updated the public website Help pages with OpenAI Codex/ChatGPT OAuth setup steps across supported locales and added website publish verification for the new OAuth help text.
- Added built-in OpenAI Codex/ChatGPT OAuth provider support in API Settings: users can log in with ChatGPT, refresh OAuth-backed model options without an API key, save OAuth configs, and run tutor LLM calls through the Codex Responses backend with secure token refresh.
- Updated the built-in DeepSeek provider to use the official canonical base URL `https://api.deepseek.com` while leaving `/v1` legacy compatibility treated as non-bug behavior.
- Fixed live API model cache refresh: successful `Test API key` probes now notify active Drift model-cache watchers so settings/session model pickers can refresh from the authoritative `/models` cache. Added active-watcher regression coverage.
- Simplified the bundled `learn` tutor prompt to plain student-visible text, added compact `student_context`, and changed app handling so learn streams/persists plain text without structured JSON schema validation/retry.
- Split review into plain `review_init` and structured `review_cont`, removed prompt-owned `next_action`/difficulty selection, and made the app persist/clamp per-KP current review difficulty based on available question-bank files. Validated with targeted prompt/schema/session tests, `flutter analyze`, and a local Windows release build.
- Fixed marketplace course/bundle deletion failures caused by dependent subject-label, quit-request, moderation, and artifact-manifest rows: full-course delete, single bundle-version delete, and automatic prune now clear those rows before deleting `bundle_versions` or `courses`. Validated remote Go tests and deployed the rebuilt API binary to production.
- Fixed teacher skill-tree target-student timing: teacher view now stores the route/assignment target student before awaiting on-demand `student_kp` materialization, so KP taps do not show a false `No assigned student` while materialization is pending. Added `test/skill_tree_page_test.dart` coverage for the pending-materialization path.
- Fixed course KP natural ordering: parser children, skill-tree search/detail lists, and persisted `course_nodes.order_index` now share dotted numeric comparison so `1.2` sorts before `1.10`. Added parser and course-import regression coverage for `1.1`, `1.2`, `1.10`, and `1.11`.
- Fixed false enrollment drift after student/teacher bundle sync: local enrollment `state2` now uses the same manifest contract as the server (`artifact_state2_v1` over sorted `artifact_id|sha256` lines), while teacher-only pending uploads still use synthetic local-only artifact ids to force the intended mismatch. Tightened due-interval regression coverage, released public client `v1.0.7`, and verified Android APK `a4baa5d899b04ea216df91e2b0c3e92e62887b74b0c38a5b5d29c497857d07b0`, Windows ZIP `ad3f83187cb311d423dfab393003b1c164ebe6352a8048ca11f17fcda75fce21`, GitHub Release `https://github.com/tutor1on1-org/tutor1on1/releases/tag/v1.0.7`, and website static sync.
- Completed the production hard cutover to artifact-manifest sync: created backup `/home/ecs-user/db_backups/family_teacher_20260401_195705_artifact_cutover_pre.sql.gz`, verified the legacy-student credential set (`albert`, `charles`), fixed `student_kp` ownership to `ftapp:ftapp`, converted `39` legacy sessions plus `3127` progress rows into `3157` canonical artifacts, dropped retired sync tables, verified old row-sync routes now return `404`, and verified `/api/artifacts/sync/state2` is live.
- Hardened release and smoke automation around the artifact cutover: moderated smoke tests can approve pending teacher/course requests with admin credentials, Windows publish retries transient candidate/canonical URL checks, and GitHub release retry preserves already-built Android/Windows artifacts during GitHub-only reruns.
- Hardened server-side artifact cutover: `cmd/artifact_cutover` now fails unless the provided `-student` credential set exactly matches legacy student usernames, converted per-KP zips are written under a run-specific cutover storage namespace, and `remote/internal/artifactsync/cutover_test.go` covers the behavior.
- Fixed teacher sync auth-loss and prompt-editor freeze regressions: token refresh now uses one shared single-flight coordinator, teacher home surfaces background auth expiry, invalid prompt saves stay inside the editor dialog, and large bundle hash/validate/metadata work is offloaded from the UI isolate.
- Added the student `Reset Local Cache From Server` recovery path in Settings through shared `StudentServerCopyService`, so duplicate-course/session recovery force-pulls enrollments, wipes local student session/progress data, and re-downloads the authoritative server copy without duplicating sync logic.
- Fixed provider-audio settings drift: successful provider model probes now replace stale TTS/STT choices, Gemini TTS-only models no longer appear in the text picker, and providers without runtime audio support do not persist incompatible audio selections.
- Fixed fresh-device progress replay and large-transfer progress visibility: importing server progress stamps both local download/upload sync state, sync overlays show transferred MB, and heavy session/progress loops yield so blocking overlays continue repainting.

Older completion details were moved to `LOGBOOK.md`.
